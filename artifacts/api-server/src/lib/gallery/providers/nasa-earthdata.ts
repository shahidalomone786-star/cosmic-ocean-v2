import { createUnavailableProvider } from "../shared";
export default createUnavailableProvider("nasa-earthdata", "NASA Earthdata", "Earthdata requires a separate account-backed search integration.");